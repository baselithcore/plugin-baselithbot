# Runtime dependencies for the Baselithbot plugin.
# Mirrors `python_dependencies` in manifest.yaml plus the framework pin.

baselith-core>=0.6.0,<1.0.0

playwright>=1.45.0
playwright-stealth>=1.0.6
pyautogui>=0.9.54
mss>=9.0.1
Pillow>=10.0.0
httpx>=0.27.0
psutil>=5.9.0
