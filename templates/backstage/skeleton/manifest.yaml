name: ${{ values.pluginName }}
version: "${{ values.version }}"
description: "${{ values.description }}"
author: "${{ values.author }}"
category: "${{ values.category }}"
readiness: "${{ values.readiness }}"
tags: ${{ values.tags | dump }}
required_resources: ${{ values.requiredResources | dump }}
optional_resources: []
python_dependencies: ${{ values.pythonDependencies | dump }}
plugin_dependencies: {}
min_core_version: null
max_core_version: null
homepage: ""
license: ""
icon: ""
screenshots: []
environment_variables: []
