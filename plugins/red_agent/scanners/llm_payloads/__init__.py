"""Curated payload corpora for the LLM-attack scanners.

Each YAML in this directory is a list of probe entries (see
``injection.yaml`` for the canonical schema). Payloads are
SPDX-clean — provenance fields point to the upstream source and
license; do not commit anything that cannot be redistributed.
"""
