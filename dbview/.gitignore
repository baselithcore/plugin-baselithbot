node_modules
# dist is tracked in the enterprise monorepo: the V3 plugin-integrity hash
# covers shipped JS, so on-host rebuilds diverge from the signed bytes.
# (Sourcemaps stay out via plugins/dbview/.gitignore.)
.next
.turbo
coverage
.env
.env.local
.env.*.local
*.log
.DS_Store
.vscode

# Runtime data (DBVIEW_DATA_DIR) — connections, history, user uploads.
# Contains encrypted secrets + user-uploaded DB blobs; never commit.
# apps/api/data/connections.json
# apps/api/data/history.json
# apps/api/data/uploads/
apps/api/data/*
